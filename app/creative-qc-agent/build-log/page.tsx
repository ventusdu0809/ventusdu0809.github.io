import Link from 'next/link';
import archive from './archive.json';
import '../case-study.css';
export const metadata = { title: 'Technical Appendix / Build Log | Du Ming', description: 'Implementation and evaluation history for the Creative QC & Repair Agent.', alternates: { canonical: '/creative-qc-agent/build-log/' } };
export default function BuildLog(){return <main className="case-study"><nav className="cs-nav" aria-label="技术附录导航"><Link href="/">← 返回作品集 / DU MING</Link><a href="/creative-qc-agent/">← 返回 Case Study</a><a href="/resume/">简历</a></nav><header className="cs-section"><p className="cs-kicker">CREATIVE QC & REPAIR AGENT</p><h1>Technical Appendix<br />/ Build Log</h1><p>Implementation and evaluation history for the Creative QC & Repair Agent.</p><p>保留原工作台的全部阶段说明、检查清单和 M01–M08（含 M05.1）报告。这里是只读历史记录；本地运行路径已替换为资源标识，原始报告及冻结结果未修改。</p><p className="cs-callout">阶段编号沿用工作台顺序，阶段与 Milestone 并非一一对应。指导语描述当时的实施过程，不代表重新运行指令。</p><nav className="cs-actions" aria-label="附录目录"><a href="#stages">阶段记录</a><a href="#reports">Milestone 报告</a><a href="/assets/creative-qc-agent/evidence/metrics.json">冻结指标</a></nav></header><section id="stages"><h2>完整阶段记录</h2>{archive.stages.map((s,i)=><article className="cs-stage" key={s.name}><p className="cs-kicker">Stage {String(i+1).padStart(2,'0')} / {s.name}</p><h3>{s.title}</h3><p>{s.intro}</p><details><summary>实施过程、检查清单与交付物</summary><p><b>当时的行动：</b>{s.action}</p><p><b>目的：</b>{s.why}</p><p>{s.deeper}</p><ul>{s.checklist.map(x=><li key={x}>{x}</li>)}</ul><p><b>交付物：</b>{s.deliverable}</p></details></article>)}</section><section id="reports" className="cs-section"><h2>M01–M08 原有报告</h2><p>展开阅读全文，包括初轮失败、最终冻结轮次和分母说明。仅对网站副本做路径脱敏。</p>{archive.reports.map(r=><details className="cs-report" key={r.name}><summary>{r.name.replaceAll('_',' ').replace('.md','')}</summary><pre>{r.text}</pre></details>)}</section><footer className="cs-footer"><a href="/creative-qc-agent/">← 返回 Case Study</a><p><Link href="/">返回作品集</Link></p></footer></main>}
